
# 🃏 智能纸牌游戏框架（CardGame Framework）

> 基于 Cocos2d-x 的模块化纸牌游戏引擎，支持关卡配置驱动、撤销重做、动画反馈与规则扩展，适用于教学演示、休闲游戏开发或 AI 交互实验平台。

---

## ✨ 核心特性

- **数据驱动关卡**：通过 `LevelConfig` + JSON 实现关卡灵活配置，支持动态加载  
- **完整 MVC 架构**：
  - **Model**：`GameModel` / `CardModel` / `UndoModel` 管理状态与历史  
  - **View**：`DeckFieldView`, `BottomFieldView`, `StackView` 等组件化 UI  
  - **Controller**：`GameController`, `DeckFieldController` 分离逻辑与表现  
- **撤销系统**：支持多步撤销（`UndoManager` + `UndoService`），记录可回滚快照  
- **动画友好**：统一 `playMoveAnimation` / `playFlipAnimation` 接口，支持回调链  
- **高扩展性**：新增玩法（如记忆翻牌、接龙）仅需扩展 `MatchService` 与 `BackupService`

---

## 📂 项目结构

```
├─ configs/          # 关卡配置（Loader + Model）
├─ models/           # 数据模型（GameModel, CardModel, UndoModel...）
├─ views/            # UI 视图（DeckFieldView, BottomFieldView, CardView...）
├─ controllers/      # 控制器（GameController, DeckFieldController...）
├─ services/         # 业务逻辑服务（MatchService, UndoService, BackupService）
├─ managers/         # 管理器（UndoManager）
└─ HelloWorld.cpp    # 游戏入口示例
```

---

## 🚀 快速开始（开发环境）

- **引擎**：Cocos2d-x 3.17+  
- **编译**：CMake 或 Cocos 命令行工具  
- **运行示例**：
  ```bash
  cocos run -p mac  # 或 windows / android
  ```

---

## 🧩 可复用模块（推荐集成）

| 模块 | 用途 | 典型场景 |
|------|------|----------|
| `CardResConfig` | 卡牌资源映射 | 换肤系统、多语言资源适配 |
| `UndoManager`   | 撤销管理 | 任意需要“后悔药”的交互应用 |
| `LevelConfigLoader` | 关卡加载 | 教育类游戏动态题库注入 |
| `CardView`      | 卡牌基类 | 快速构建新卡牌玩法 UI |
